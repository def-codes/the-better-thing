{
  "extends": "../tsconfig-composite",
  "references": [
    /* For file system watcher, which should be elsewhere */
    { "path": "../process-trees" },
    /* For traversal, which should be elsewhere */
    { "path": "../graphviz-format" }
  ]
}
