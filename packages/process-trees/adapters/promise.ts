// maybe this will be folded into something more basic (because it is built-in)
// but note that a Promise is a state machine
// with some common aspects of a process lifecycle
// it is a kind of special case of an asynchronous event stream
// that fires only one time
//
// STATE MACHINE
// states:
// - pending (initial)
// - done (terminal)
// - error (terminal)
