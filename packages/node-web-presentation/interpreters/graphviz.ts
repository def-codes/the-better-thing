// Graphviz interpreter.  Bear with me.
export const graphviz_interpreter = () => {
  // la la la
  // is this about the whole launching a browser thing?
  // or just somehow getting the graphviz output in front of the person
};
